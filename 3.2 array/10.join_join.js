// join :- it join all the elements in array or we can say it put the given elements between the array

let arr = [1,2,3,4,5]

console.log(arr.join("&")); // symbol and letter is used in string 
console.log(arr.join(2)); // number we can use directly 